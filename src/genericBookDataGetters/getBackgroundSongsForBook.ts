import type { BackgroundSongForBook } from "@/types/book";

export const getBackgroundSongsForBook = (): BackgroundSongForBook[] => {
  throw new Error("getBackgroundSongsForBook should never be called at runtime");
};
