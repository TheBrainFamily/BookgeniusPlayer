import type { BackgroundForBook } from "@/types/book";

export const getBackgroundsForBook = (): BackgroundForBook[] => {
  throw new Error("getBackgroundsForBook should never be called at runtime");
};
