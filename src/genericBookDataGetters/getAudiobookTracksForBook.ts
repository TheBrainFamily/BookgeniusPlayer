import type { AudiobookTracksSection } from "@/types/book";

export const getAudiobookTracksForBook = (): AudiobookTracksSection[] => {
  throw new Error("getAudiobookTracksForBook should never be called at runtime");
};
