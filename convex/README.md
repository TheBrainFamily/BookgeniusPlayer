# Convex Backend

Convex functions for BookGenius - handles book queries, user data, asset management, and AI content generation.

See `schema.ts` for the data model and individual `.ts` files for queries/mutations.
