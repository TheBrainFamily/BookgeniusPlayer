/**
 * Splash screen transition timing constants.
 *
 * IMPORTANT: This is the single source of truth for splash transition duration.
 * The CSS variable --splash-fade-duration in SplashScreen.css must match this value.
 */
export const SPLASH_FADE_DURATION_MS = 1000;
